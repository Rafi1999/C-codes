#include<stdio.h>
int main()
{
    int a,b,x,c1=0;

    while(1)
    {
        scanf("%d %d",&a,&b);
    printf("Novo grenal (1-sim 2-nao)");
        c1+=1;
        scanf("%d",x);
        if(x!=1)
            break;
        else
            continue
            scanf("%d %d",)
    }
    printf("%d grenais",c1);
}
